spring:
  jpa:
    show-sql: true
    generate-ddl: true
    open-in-view: false
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:h2:mem:testDb
    username: "sa"
    password: "sa"
    driver-class-name: org.h2.Driver
  mail:
    host: "smtp.gmail.com"
    port: 0
    username: ""
    password: ""
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

minio:
  url: "url"
  bucket:
    name: "bucketname"
  access:
    key: "key"
    secret: "secret"

stripe:
  secret-key: ""
  secret-endpoint: ""

security:
  token:
    secret: secret_jwt_token_test

front:
  base-url: ""